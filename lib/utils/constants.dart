class Constants {
  // App constants
  static const String appName = 'Loan Manager App';
  static const String appVersion = '1.0.0';

  // API endpoints (for future use)
  static const String baseUrl = 'https://api.example.com';

  // Default values
  static const double defaultInterestRate = 10.0;
  static const int defaultTenure = 12;

  // UI constants
  static const double cardElevation = 4.0;
  static const double padding = 16.0;
  static const double borderRadius = 8.0;

  // Colors (if not using theme)
  // static const Color primaryColor = Colors.blue;
  // static const Color secondaryColor = Colors.green;

  // Twilio SMS API constants (configured in settings)
  // These are now managed by SettingsProvider
}
